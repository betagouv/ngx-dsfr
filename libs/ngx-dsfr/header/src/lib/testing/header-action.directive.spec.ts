/**
 * Internal imports
 */
import { DsfrHeaderActionDirective } from '../header-action.directive';
import { TemplateRef } from '@angular/core';

describe('DsfrHeaderActionDirective', () => {
  // TODO: Rewrite with https://angular.io/guide/testing-attribute-directives
  xit('should create an instance', () => {
    /*const directive = new DsfrHeaderActionDirective(new TemplateRef());
    expect(directive).toBeTruthy();*/
  });
});
